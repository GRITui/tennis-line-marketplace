# apps/admin (Next.js admin panel)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
NEXT_PUBLIC_LIFF_ID=
# Server-only shared password gating the admin panel (/login). Required —
# with it unset every page redirects to /login and login answers 503.
ADMIN_PASSWORD=